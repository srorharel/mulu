{
  "name": "mulu-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MULU marketing site — source for mulu-site-cloudflare (Cloudflare Pages).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "sync": "node scripts/sync-to-cloudflare.mjs",
    "stage": "vite build && node scripts/sync-to-cloudflare.mjs",
    "deploy": "vite build && node scripts/sync-to-cloudflare.mjs && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "framer-motion": "^11.11.0",
    "lucide-react": "^0.456.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.3.3",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0"
  }
}
